<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
    body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background: #1f8384;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

header {
    background-color: #e8e8e8;
    color: #1f8384;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2rem 4rem;
    height: 30px;
    width: 100%;
    box-sizing: border-box;
}

.logo {
    font-size: 2.5rem;
    font-weight: bold;
}

form {
    background: #fff;
    padding: 2rem 2.5rem;
    border-radius: 10px;
    box-shadow: 0 4px 16px rgba(0,0,0,0.08);
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
    min-width: 320px;
    margin-top: 2rem;
}

input[type="text"] {
    padding: 0.7rem;
    border: 1px solid #1f8384;
    border-radius: 5px;
    font-size: 1rem;
}

h2{
    color: #1f8384;
}
button[type="submit"] {
    background: #1f8384;
    color: #fff;
    border: none;
    padding: 0.8rem;
    border-radius: 5px;
    font-size: 1rem;
    cursor: pointer;
    transition: background 0.2s;
}

button[type="submit"]:hover {
    background: #16696a;
}</style>
</head>
<body>
    <form method="POST" action="/verify-otp">
        <h2>please enter OTP which has sent to your mail id</h2>
        <input type="hidden" name="email" value="<%=email%>">
    <input type="text" name="otp" id="otp">
    <button type="submit">submit</button>
</form>
   
</body>
</html>